# Sony SED-E1 connection config
# bt_address=auto means: scan paired devices for any SmartEyeglass
# bt_address=ac:9b:0a:37:a6:c6   # pin to a specific device
bt_address=auto
rfcomm_channel=4
capture_log=/tmp/glasses_capture.log
